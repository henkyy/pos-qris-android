'use client'

import { useCallback, useEffect, useMemo, useState } from 'react'
import Button from '../../components/ui/Button'
import Modal from '../../components/ui/Modal'
import { getActiveWorkspace } from '../../lib/business-context'
import { requireSupabase } from '../../lib/supabase'
import styles from './ProductWorkspace.module.css'

type Opt={id:string;name:string}; type Unit={id?:string;unit_id:string;conversion_to_base:number;is_purchase_unit:boolean;is_sales_unit:boolean}; type Price={id?:string;unit_id:string;min_qty:number;price:number;discount_percent:number;valid_until?:string|null};
type Product={id:string;name:string;sku:string;barcode:string|null;category_id:string|null;base_unit_id:string;current_cost:number;last_purchase_cost:number;min_stock:number;reorder_point:number;is_active:boolean;category?:string;base_unit?:string;units:Unit[];prices:Price[]}
const rp=(v:number)=>`Rp ${new Intl.NumberFormat('id-ID').format(Number(v)||0)}`
export default function ProductCatalog(){
 const db=useMemo(()=>requireSupabase(),[]);const [rows,setRows]=useState<Product[]>([]),[cats,setCats]=useState<Opt[]>([]),[units,setUnits]=useState<Opt[]>([]),[priceList,setPriceList]=useState('');const [loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(''),[q,setQ]=useState(''),[open,setOpen]=useState(false),[editing,setEditing]=useState<Product|null>(null);const [f,setF]=useState<any>({});const [us,setUs]=useState<Unit[]>([]),[ps,setPs]=useState<Price[]>([])
 const load=useCallback(async()=>{setLoading(true);setError('');try{const w=await getActiveWorkspace();const [{data:p,error:pe},{data:c,error:ce},{data:u,error:ue},{data:pl,error:ple}]=await Promise.all([db.from('products').select('*').eq('business_id',w.business.id).order('name'),db.from('categories').select('id,name').eq('business_id',w.business.id).eq('is_active',true).order('name'),db.from('units').select('id,name,symbol').eq('business_id',w.business.id).order('name'),db.from('price_lists').select('id').eq('business_id',w.business.id).eq('is_default',true).eq('is_active',true).limit(1)]);if(pe)throw pe;if(ce)throw ce;if(ue)throw ue;if(ple)throw ple;const lid=pl?.[0]?.id||'';setPriceList(lid);const ids=(p||[]).map((x:any)=>x.id);const [{data:pu,error:pue},{data:pp,error:ppe}]=await Promise.all([ids.length?db.from('product_units').select('*').in('product_id',ids):Promise.resolve({data:[],error:null}),ids.length&&lid?db.from('product_prices').select('*').eq('price_list_id',lid).in('product_id',ids):Promise.resolve({data:[],error:null})]);if(pue)throw pue;if(ppe)throw ppe;const um=new Map((u||[]).map((x:any)=>[x.id,x]));const cm=new Map((c||[]).map((x:any)=>[x.id,x.name]));const ub=new Map<string,Unit[]>(),pb=new Map<string,Price[]>();for(const x of pu||[]){const a=ub.get(x.product_id)||[];a.push(x);ub.set(x.product_id,a)}for(const x of pp||[]){const a=pb.get(x.product_id)||[];a.push(x);pb.set(x.product_id,a)}setRows((p||[]).map((x:any)=>({...x,category:cm.get(x.category_id)||'-',base_unit:um.get(x.base_unit_id)?.name||'-',units:ub.get(x.id)||[],prices:pb.get(x.id)||[]})));setCats(c||[]);setUnits((u||[]).map((x:any)=>({id:x.id,name:x.symbol?`${x.name} (${x.symbol})`:x.name})))}catch(e:any){setError(e?.message||'Gagal memuat produk.')}finally{setLoading(false)}},[db]);useEffect(()=>{load()},[load])
 const filtered=rows.filter(x=>`${x.name} ${x.sku} ${x.barcode||''} ${x.category}`.toLowerCase().includes(q.toLowerCase()));
 function create(){setEditing(null);setF({name:'',sku:'',barcode:'',category_id:'',base_unit_id:'',current_cost:'0',min_stock:'0',reorder_point:'0'});setUs([]);setPs([]);setError('');setOpen(true)}
 function edit(p:Product){setEditing(p);setF({name:p.name,sku:p.sku,barcode:p.barcode||'',category_id:p.category_id||'',base_unit_id:p.base_unit_id,current_cost:String(p.current_cost),min_stock:String(p.min_stock),reorder_point:String(p.reorder_point)});setUs(p.units.map(x=>({...x})));setPs(p.prices.map(x=>({...x})));setError('');setOpen(true)}
 function addUnit(){if(us.length<3)setUs(a=>[...a,{unit_id:'',conversion_to_base:1,is_purchase_unit:false,is_sales_unit:false}])}function addPrice(){setPs(a=>[...a,{unit_id:f.base_unit_id||'',min_qty:1,price:0,discount_percent:0}])}
 async function save(){if(!f.name?.trim()||!f.sku?.trim()||!f.base_unit_id){setError('Nama, SKU dan satuan dasar wajib diisi.');return}const list=us.filter(x=>x.unit_id);if(!list.some(x=>x.unit_id===f.base_unit_id))list.unshift({unit_id:f.base_unit_id,conversion_to_base:1,is_purchase_unit:true,is_sales_unit:true});if(list.some(x=>Number(x.conversion_to_base)<=0)||new Set(list.map(x=>x.unit_id)).size!==list.length){setError('Satuan harus unik dan konversinya harus lebih dari 0.');return}if(!priceList){setError('Price list default belum tersedia.');return}setSaving(true);setError('');try{const w=await getActiveWorkspace();let id=editing?.id;const payload={name:f.name.trim(),sku:f.sku.trim(),barcode:f.barcode?.trim()||null,category_id:f.category_id||null,base_unit_id:f.base_unit_id,product_type:'GOODS',track_batch:false,track_expiry:false,min_stock:Number(f.min_stock||0),reorder_point:Number(f.reorder_point||0),cost_method:'MOVING_AVERAGE',current_cost:Number(f.current_cost||0),last_purchase_cost:editing?.last_purchase_cost||0,is_active:editing?.is_active??true};if(id){const r=await db.from('products').update(payload).eq('id',id).eq('business_id',w.business.id);if(r.error)throw r.error}else{const r=await db.from('products').insert({...payload,business_id:w.business.id}).select('id').single();if(r.error)throw r.error;id=r.data.id}if(!id)throw new Error('ID produk tidak ditemukan.');for(const x of list){const r=await db.from('product_units').upsert({product_id:id,unit_id:x.unit_id,conversion_to_base:Number(x.conversion_to_base),is_purchase_unit:!!x.is_purchase_unit,is_sales_unit:!!x.is_sales_unit},{onConflict:'product_id,unit_id'});if(r.error)throw r.error}for(const x of ps.filter(z=>z.unit_id&&Number(z.price)>=0)){const r=x.id?await db.from('product_prices').update({unit_id:x.unit_id,min_qty:Number(x.min_qty||1),price:Math.round(Number(x.price||0)),discount_percent:Number(x.discount_percent||0),valid_until:x.valid_until||null}).eq('id',x.id):await db.from('product_prices').insert({price_list_id:priceList,product_id:id,unit_id:x.unit_id,min_qty:Number(x.min_qty||1),price:Math.round(Number(x.price||0)),discount_percent:Number(x.discount_percent||0),valid_from:new Date().toISOString(),valid_until:null});if(r.error)throw r.error}setOpen(false);await load()}catch(e:any){setError(e?.message||'Gagal menyimpan produk.')}finally{setSaving(false)}}
 return <div className="module-page"><div className={styles.hero}><div><span className="eyebrow">MASTER · PRODUK</span><h1>Produk</h1><p>Identitas, kategori, HPP, harga jual, satuan dan konversi dikelola dalam satu tempat.</p></div><Button onClick={create}>+ Tambah Produk</Button></div><div className={styles.summary}><div><span>Total produk</span><strong>{rows.length}</strong></div><div><span>Harga terisi</span><strong>{rows.filter(x=>x.prices.length).length}</strong></div><div><span>Multi-satuan</span><strong>{rows.filter(x=>x.units.length>1).length}</strong></div><div><span>Produk aktif</span><strong>{rows.filter(x=>x.is_active).length}</strong></div></div>{error&&<div className="module-alert">{error}</div>}<section className="module-card"><div className={styles.toolbar}><div><strong>Daftar Produk</strong><span>{loading?'Memuat…':`${filtered.length} produk`}</span></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari nama, SKU, barcode…"/><Button variant="secondary" onClick={load}>Muat ulang</Button></div>{loading?<div className={styles.empty}>Memuat katalog…</div>:<div className="ui-table-wrap"><table className="ui-table"><thead><tr><th>Produk</th><th>Kategori</th><th>HPP</th><th>Harga jual</th><th>Satuan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>{filtered.map(p=><tr key={p.id}><td><strong>{p.name}</strong><small>{p.sku}{p.barcode?` · ${p.barcode}`:''}</small></td><td>{p.category}</td><td>{rp(p.current_cost)}</td><td>{p.prices.length?p.prices.map(x=>`${x.unit_id===p.base_unit_id?'Dasar':(p.units.find(u=>u.unit_id===x.unit_id)?.unit_id||'Unit')}: ${rp(x.price)}`).join(' · '):<span className={styles.missing}>Belum diatur</span>}</td><td>{p.base_unit} · {p.units.length} unit</td><td>{p.is_active?'Aktif':'Nonaktif'}</td><td><button className="table-edit" onClick={()=>edit(p)}>Edit</button></td></tr>)}</tbody></table></div>}</section><Modal open={open} title={editing?`Edit Produk · ${editing.name}`:'Tambah Produk'} onClose={()=>setOpen(false)}><div className={styles.form}><div className={styles.section}><h3>Identitas</h3><div className={styles.grid}><label>Nama *<input value={f.name||''} onChange={e=>setF({...f,name:e.target.value})}/></label><label>SKU *<input value={f.sku||''} onChange={e=>setF({...f,sku:e.target.value})}/></label><label>Barcode<input value={f.barcode||''} onChange={e=>setF({...f,barcode:e.target.value})}/></label><label>Kategori<select value={f.category_id||''} onChange={e=>setF({...f,category_id:e.target.value})}><option value="">Tanpa kategori</option>{cats.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label></div></div><div className={styles.section}><h3>HPP & stok</h3><p>HPP awal boleh diisi untuk opening stock. Setelah pembelian, Moving Average memperbarui HPP secara otomatis.</p><div className={styles.grid}><label>HPP / unit<input type="number" min="0" value={f.current_cost} onChange={e=>setF({...f,current_cost:e.target.value})}/></label><label>Minimum stok<input type="number" min="0" value={f.min_stock} onChange={e=>setF({...f,min_stock:e.target.value})}/></label><label>Titik pesan ulang<input type="number" min="0" value={f.reorder_point} onChange={e=>setF({...f,reorder_point:e.target.value})}/></label><div className={styles.readonly}><span>Metode</span><strong>Moving Average</strong></div></div></div><div className={styles.section}><div className={styles.sectionHead}><div><h3>Satuan & konversi</h3><p>Contoh: PCS 1, BOX 12, DUS 144. Maksimal 3 unit per produk pada UI.</p></div><Button variant="secondary" onClick={addUnit} disabled={us.length>=3}>+ Satuan</Button></div>{us.map((u,i)=><div className={styles.unitRow} key={u.id||i}><select value={u.unit_id} onChange={e=>setUs(a=>a.map((x,j)=>j===i?{...x,unit_id:e.target.value}:x))}><option value="">Pilih satuan</option>{units.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><input type="number" min="0.000001" step="any" value={u.unit_id===f.base_unit_id?1:u.conversion_to_base} disabled={u.unit_id===f.base_unit_id} onChange={e=>setUs(a=>a.map((x,j)=>j===i?{...x,conversion_to_base:Number(e.target.value)}:x))}/><label className={styles.check}><input type="checkbox" checked={u.is_purchase_unit} onChange={e=>setUs(a=>a.map((x,j)=>j===i?{...x,is_purchase_unit:e.target.checked}:x))}/> Beli</label><label className={styles.check}><input type="checkbox" checked={u.is_sales_unit} onChange={e=>setUs(a=>a.map((x,j)=>j===i?{...x,is_sales_unit:e.target.checked}:x))}/> Jual</label></div>)}</div><div className={styles.section}><div className={styles.sectionHead}><div><h3>Harga jual</h3><p>Harga per satuan. Riwayat harga tidak dihapus saat harga baru disimpan.</p></div><Button variant="secondary" onClick={addPrice} disabled={!f.base_unit_id}>+ Harga</Button></div>{ps.map((x,i)=><div className={styles.priceRow} key={x.id||i}><select value={x.unit_id} onChange={e=>setPs(a=>a.map((z,j)=>j===i?{...z,unit_id:e.target.value}:z))}><option value="">Pilih satuan</option>{us.filter(u=>u.unit_id).map(u=><option key={u.unit_id} value={u.unit_id}>{units.find(v=>v.id===u.unit_id)?.name||'Satuan'}</option>)}</select><input type="number" min="0" value={x.price} onChange={e=>setPs(a=>a.map((z,j)=>j===i?{...z,price:Number(e.target.value)}:z))} placeholder="Harga jual"/><input type="number" min="1" value={x.min_qty} onChange={e=>setPs(a=>a.map((z,j)=>j===i?{...z,min_qty:Number(e.target.value)}:z))} placeholder="Min qty"/></div>)}{!ps.length&&<div className={styles.hint}>Belum ada harga. Tambahkan harga sebelum produk digunakan di kasir.</div>}</div><div className={styles.actions}><Button variant="secondary" onClick={()=>setOpen(false)} disabled={saving}>Batal</Button><Button onClick={save} disabled={saving}>{saving?'Menyimpan…':'Simpan Produk'}</Button></div></div></Modal></div>
}
